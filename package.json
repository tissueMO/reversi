{
  "name": "reversi-game",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "yarn dev",
    "build": "yarn build",
    "generate": "yarn generate",
    "preview": "yarn preview",
    "lint": "yarn lint"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "eslint": "^8.28.0",
    "nuxt": "^3.8.0",
    "typescript": "^5.0.0"
  }
}
